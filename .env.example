# Project ini menggunakan Firebase (config hardcoded di src/lib/firebase.ts).
# Tidak ada environment variable yang wajib di-set untuk build & deploy.
#
# Opsional: override basePath kalau repo GitHub bukan di root user pages.
# NEXT_PUBLIC_BASE_PATH=/nama-repo
